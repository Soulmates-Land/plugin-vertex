{
  "name": "@elizaos/plugin-vertex",
  "version": "0.5.0",
  "description": "Google Vertex AI plugin for ElizaOS — Claude and Gemini models via GCP",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@ai-sdk/google-vertex": "^4.0.0",
    "@google-cloud/speech": "^7.3.0",
    "@google-cloud/text-to-speech": "^6.4.0",
    "ai": "^6.0.23"
  },
  "devDependencies": {
    "@elizaos/core": "2.0.0-alpha.33",
    "@types/node": "^25.6.0",
    "typescript": "^5.9.0",
    "vitest": "^4.1.5"
  },
  "peerDependencies": {
    "@elizaos/core": ">=2.0.0-alpha.30"
  },
  "agentConfig": {
    "pluginType": "elizaos:plugin:1.0.0",
    "pluginParameters": {
      "GOOGLE_VERTEX_PROJECT_ID": {
        "type": "string",
        "description": "Google Cloud project ID with Vertex AI API enabled",
        "required": true
      },
      "GOOGLE_VERTEX_REGION": {
        "type": "string",
        "description": "Vertex AI region (e.g. us-east5 for Claude, global for Gemini). Defaults to us-central1.",
        "required": false
      },
      "GOOGLE_SPEECH_REGION": {
        "type": "string",
        "description": "Vertex AI Speech-to-Text region (e.g. us-central1 for chirp). Defaults to us-central1.",
        "required": false
      },
      "VERTEX_SMALL_MODEL": {
        "type": "string",
        "description": "Model for TEXT_SMALL — claude-* or gemini-* (provider auto-detected by prefix)",
        "default": "claude-haiku-4-5",
        "required": false
      },
      "VERTEX_LARGE_MODEL": {
        "type": "string",
        "description": "Model for TEXT_LARGE — claude-* or gemini-* (provider auto-detected by prefix)",
        "default": "claude-sonnet-4-6",
        "required": false
      },
      "VERTEX_REASONING_SMALL_MODEL": {
        "type": "string",
        "description": "Model for TEXT_REASONING_SMALL — claude-* or gemini-* (provider auto-detected by prefix)",
        "default": "claude-sonnet-4-6",
        "required": false
      },
      "VERTEX_REASONING_LARGE_MODEL": {
        "type": "string",
        "description": "Model for TEXT_REASONING_LARGE — claude-* or gemini-* (provider auto-detected by prefix)",
        "default": "claude-opus-4-6",
        "required": false
      }
    }
  },
  "license": "MIT",
  "keywords": ["elizaos", "plugin", "vertex-ai", "google-cloud", "claude", "anthropic", "gemini", "google"]
}
